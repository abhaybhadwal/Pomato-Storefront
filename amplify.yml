version: 1
frontend:
  phases:
    # No build step needed for this static site
    build:
      commands: []
  artifacts:
    # Deploy all files from the root
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
